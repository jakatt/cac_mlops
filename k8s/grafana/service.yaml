apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: cac-mlops
  annotations:
    service.beta.kubernetes.io/scw-loadbalancer-type: "LB-S"
spec:
  type: LoadBalancer
  selector:
    app: grafana
  ports:
  - port: 3000
    targetPort: 3000
