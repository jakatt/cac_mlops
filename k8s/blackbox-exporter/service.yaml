apiVersion: v1
kind: Service
metadata:
  name: blackbox-exporter
  namespace: cac-mlops
  labels:
    app: blackbox-exporter
spec:
  type: ClusterIP
  selector:
    app: blackbox-exporter
  ports:
  - port: 9115
    targetPort: 9115
    name: http
