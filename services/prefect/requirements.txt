# API ML stack
fastapi>=0.111.0
mlflow>=3.1.0
boto3>=1.34.0
scikit-learn>=1.4.0
pandas>=2.2.0
numpy>=1.26.0
joblib>=1.4.0
requests>=2.31.0

# Client Gradio — appelle réellement la fonction "Predict" exposée par
# gradio-public (src/flows/test_gradio_public_flow.py), pas juste un ping
# /health — seule façon de tester fonctionnellement l'accès réellement
# utilisé par les utilisateurs finaux (cf. observabilité par accès, PR230).
gradio_client>=1.0.0

# Orchestration
prefect>=3.0.0
