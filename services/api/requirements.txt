fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
mlflow>=3.9.0
boto3>=1.34.0
scikit-learn>=1.4.0
xgboost>=2.0.0
lightgbm>=4.0.0
pandas>=2.2.0
numpy>=1.26.0
joblib>=1.4.0
prometheus-client>=0.20.0
requests>=2.31.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
python-multipart>=0.0.9
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
PyYAML>=6.0
prefect>=3.0
evidently>=0.4.0,<0.5.0
# plotly<6.0.0 : plotly.py>=6.0.0 encode les données des graphiques en
# base64 compact (clés "dtype"/"bdata") au lieu de tableaux JSON classiques
# ("x": [0, 1, ...]) — le Plotly.js embarqué par evidently 0.4.40 dans le
# HTML généré (report.save_html()) ne sait pas décoder ce nouveau format :
# les graphiques interactifs des rapports Evidently se rendent VIDES (axes
# affichés, aucune barre) alors que les données sont bien présentes dans le
# JSON. Vérifié empiriquement : même code, seule la version de plotly
# change, "x": [0, 1] (5.24.1, OK) vs "x": {"dtype":"i1","bdata":"AAE="}
# (6.9.0, cassé). evidently ne pin pas plotly lui-même (dépendance
# transitive), d'où la résolution vers 6.x sur un build récent.
plotly>=5.18.0,<6.0.0
dvc[s3]>=3.0.0
docker>=7.0.0
pandera>=0.19.0

# Client Gradio — prefect-worker exécute src/flows/test_gradio_public_flow.py
# (appel réel de la fonction Predict de gradio-public, pas un ping /health).
# prefect-worker tourne sur l'image cac-mlops-api (docker-compose.yml —
# services/prefect/ n'est référencé par aucun build, mort depuis une
# précédente itération), donc c'est CE fichier qui compte, pas
# services/prefect/requirements.txt (corrigé après un premier essai raté :
# ModuleNotFoundError sur le premier déploiement de test_gradio_public_flow,
# 2026-07-28).
gradio_client>=1.0.0
